#include"main.h"
/**
 * add - add function
 * @m: input for add function
 * @n: input for add  function
 * Return: Always 0 (Success)
*/
int add(int n, int m)
{
	int sum;

	sum = n + m;
	return (sum);
}

